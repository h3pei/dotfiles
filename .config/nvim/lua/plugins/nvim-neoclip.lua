require("neoclip").setup({
  history = 100,
  preview = false,
  keys = {
    telescope = {
      i = {
        select = "<Space>",
        paste = "<CR>",
      },
      n = {
        select = "<Space>",
        paste = "<CR>",
      },
    },
  },
})
