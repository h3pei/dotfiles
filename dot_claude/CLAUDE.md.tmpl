## Conversation rules

- 常に日本語で会話する

## Git rules

- default ブランチに force push してはいけない (MUST)
- default ブランチに無許可に push してはいけない。許可があればOK
{{- if stat (joinPath .chezmoi.homeDir .obsidianpath) }}

## Obsidian への記録

- 再利用価値のある発見・意思決定が出たら、依頼がなくても ai-brain-recorder skill を使って記録する。
  - 一時的/自明な内容は記録しない
- 1記録1ファイル
- 種別・主題は frontmatter の tags で表す
- 記録したことは報告する
{{- end }}
